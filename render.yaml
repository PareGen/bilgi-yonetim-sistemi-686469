# Schema: app_bilgi_yonetim_sistemi_686469
# Connection: (configured via environment)

services:
  - type: web
    name: bilgi-yonetim-sistemi-686469-api
    runtime: node
    region: oregon
    plan: free
    buildCommand: cd apps/api && npm install && npm run build
    startCommand: "cd apps/api && npm run start:prod"
    healthCheckPath: /api/v1/health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        value: "postgresql://saas_template_db_user:3TIjtXU0ylx4mulMYaRjGOMtaeRx9xDG@dpg-d435bujipnbc73bgsfag-a.frankfurt-postgres.render.com/saas_template_db?schema=app_bilgi_yonetim_sistemi_686469"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: JWT_SECRET
        generateValue: true
      - key: DATABASE_SCHEMA
        value: app_bilgi_yonetim_sistemi_686469
  - type: web
    name: bilgi-yonetim-sistemi-686469-frontend
    runtime: static
    region: oregon
    buildCommand: cd apps/frontend && npm install && npm run build
    staticPublishPath: apps/frontend/out
    pullRequestPreviewsEnabled: false
    autoDeploy: true
    envVars:
      - key: NEXT_PUBLIC_API_URL
        fromService:
          name: bilgi-yonetim-sistemi-686469-api
          type: web
          envVarKey: RENDER_EXTERNAL_URL
    headers:
      - path: /*
        name: X-Frame-Options
        value: DENY
      - path: /*
        name: X-Content-Type-Options
        value: nosniff
    routes:
      - type: rewrite
        source: /*
        destination: /index.html